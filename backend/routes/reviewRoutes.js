// routes/reviewRoutes.js
const express = require('express');
const router = express.Router();
module.exports = router; // Reviews handled in product routes

// routes/adminRoutes.js - export separately
